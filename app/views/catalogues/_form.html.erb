<%= form_for(@catalogue) do |f| %>
  <% if @catalogue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@catalogue.errors.count, "error") %> prohibited this catalogue from being saved:</h2>

      <ul>
      <% @catalogue.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
